<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fieldtester"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:configChanges="keyboardHidden|orientation"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >


        <activity
            android:name="com.example.fieldtester.MainScreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All Article Activity -->
        <activity
            android:name="com.example.fieldtester.AllArticlesActivity"
            android:label="Articles" >

        </activity>

        <!-- View Article Activity -->
        <activity
            android:name="com.example.fieldtester.ViewArticleActivity"
            android:label="Articles" >

        </activity>

        <!-- All Product Activity -->
        <activity
            android:name="com.example.fieldtester.AllProductsActivity"
            android:label="All Products" >
        </activity>

        <!-- Add Product Activity -->
        <activity
            android:name="com.example.fieldtester.NewProductActivity"
            android:label="Add New Product" >
        </activity>

        <!-- Edit Product Activity -->
        <activity
            android:name="com.example.fieldtester.EditProductActivity"
            android:label="Edit Product" >
        </activity>


    </application>

    <!--  Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>